# V18.1 Formal Parity Runtime Remediation
Status: planned_follow_up
Predecessor: v13.27.1.18
Trigger: terminal one-shot V18 formal run failure

## Frozen V18 Outcome

- Freeze commit: `aa2df4b5e8fc4e9c447edd3c5fef0a03de26ec01`
- Implementation commit: `597e34aa583224a3801dba38fef06e8f8a1734ee`
- Campaign: `advisory_r_v18_s01_capital_policy_correction_7ec0b57a7093dc7a`
- Candidate: `s01_bear_idiosyncratic_selloff_recovery_4h`
- Formal run claims: 1
- Result reads: 0
- Formal result artifacts: none
- Terminal failure: `NameError`

The frozen S01 adapter parity path calls `_signal_id` when a real adapter signal is
produced, but that name is not defined in `formal_parity.py`. The one-shot ledger is
terminal and V18 must not be patched or rerun in place.

## Required V18.1 Work

1. Create a new campaign ID, preregistration hash, implementation commit, freeze
   commit, and tag. Do not mutate the V18 preregistration or ledger.
2. Replace the undefined S01 identity call with the existing S01 identity helper.
3. Add a real-input parity test whose adapter emits at least one signal, so the
   signal identity branch executes before any new preregistration is frozen.
4. Retain the candidate-neutral Formal Validation Core and the shared
   `CandidateAdapter` contract.
5. Retain independently versioned Capacity, Cluster, Beta, and Ranking policy
   objects.
6. Retain dynamic `campaignId/candidateId` artifact paths and generic runner
   arguments.
7. Preserve S01, capital-policy numeric values, ExitPolicy, gates, universe,
   costs, and split unless a separately approved campaign explicitly changes
   them.
8. Repeat the complete pre-result test suite and remote-freeze audit before the
   next one-shot formal claim.

## Safety Boundary

- No Release creation.
- No Demo ARM.
- No order creation.
- No credential access.
- No locked OOS access.
- No result-driven parameter repair.

# AlphaPilot V13.27.1.34B - Funding, PIT Metadata, and Resumable Forward Collection

## Objective

Extend the accepted V34A OKX public-data warehouse without mutating its frozen candle snapshot. Add auditable recent funding history, point-in-time instrument metadata, and a resumable one-shot forward collector for public derivatives state.

## Boundaries

- Public OKX endpoints only; no API key, account, position, order, Trade API, or Withdraw API.
- Reuse `D:\Codex-Workspace\回测数据\okx_official_v1` and preserve every V34A snapshot and partition byte-for-byte.
- No candidate generation, Formal run, result read, Release, approval, Demo ARM, or order side effect.
- Do not reconstruct historical instrument state that was not observed. PIT history begins at collection time.
- Funding `realizedRate` is available only at or after `fundingTime`; it is never treated as a pre-funding predictor.

## Deliverables

1. Canonical recent funding partitions for BTC/ETH/SOL perpetual swaps with source hashes and causal availability timestamps.
2. Immutable daily SWAP instrument metadata snapshots with documented product fields and upcoming parameter changes.
3. Append-only forward snapshots for instrument state, funding, open interest, mark price, index price, ticker spread, and public order-book summary where available.
4. Atomic checkpoints that support idempotent reruns and resume after an interrupted stream.
5. A composite V34B manifest and a V33 program receipt with all trading and research side-effect counters fixed at zero.
6. Tests, real public-data pilot evidence, safety scan, documentation, commits, tags, and pushes.

## Implementation Sequence

1. Add failing contract tests for funding normalization, PIT metadata, forward snapshots, checkpoint resume, immutable manifests, and zero side effects.
2. Extend `OkxPublicClient` only with missing public endpoint wrappers.
3. Implement a focused V34B collector beside the V34A candle pilot.
4. Add a CLI that accepts warehouse/program roots and supports deterministic bounded instruments.
5. Run the real BTC/ETH/SOL pilot twice, proving checkpoint reuse and V34A byte preservation.
6. Run targeted and full tests, compile checks, configuration validation, and safety scans.

## Acceptance Gates

- Funding, metadata, and every enabled forward stream produce hashed artifacts or an explicit unavailable status.
- A rerun does not duplicate canonical rows or PIT snapshots.
- An interrupted stream resumes from its checkpoint without repeating completed streams.
- The V34A snapshot manifest and its nine canonical partitions retain their original SHA-256 hashes.
- Candidate, Formal, result-read, Release, approval, Demo ARM, and order counts remain zero.
- Full test suite and repository hygiene checks pass.

# V13.27.1.17 Phase 2 Engineering Readiness

## Objective

Remove the remaining non-performance engineering blockers for the frozen S01
formal workflow without running formal Walk-forward, reading Future Locked OOS
content, producing Formal Evidence, creating a Release, or touching Demo/order
paths.

## Immutable boundary

- Candidate remains `s01_bear_idiosyncratic_selloff_recovery_4h`.
- Formal preregistration, universe, five folds, purge/embargo, capital policy,
  costs, benchmarks, and exit policy remain unchanged.
- Runtime is the already-present image
  `freqtradeorg/freqtrade@sha256:87aa5c6d65359b34e9d99a0bb260a38c0efe0315253811e6f48c2afe8f278a6a`.
- Runtime smoke and parity use synthetic or non-result fixtures only, with
  Docker networking disabled.
- Future Locked OOS stays on route `future_data_required`; only its identity,
  boundary, and zero-access ledger may be frozen.
- New performance metrics, formal results, Locked OOS reads, Formal Evidence,
  Release, Demo ARM, and orders all remain zero.

## TDD implementation sequence

### 1. Pinned Freqtrade runtime

1. Add failing tests for an immutable digest reference, exact Freqtrade/Python/
   CCXT versions, UTC, deterministic seeds, dependency lock hashing, and a
   network-disabled smoke result.
2. Implement `alphapilot/formal_validation/freqtrade_runtime.py` and
   `scripts/audit_freqtrade_runtime.py`.
3. Run the exact local image with `--network none`; prove CLI startup, config
   parsing, S01 strategy loading, and callable exit hooks without market data,
   credentials, account access, or Locked OOS access.
4. Write `freqtrade_runtime_manifest.json`,
   `freqtrade_dependency_lock.txt`, and `freqtrade_runtime_smoke.json`.

### 2. Complete synthetic dual-engine parity

1. Add failing tests covering entry identity, initial stop, every exit leg,
   fractions, exit reasons, trigger/execution timestamps, and numeric tolerance.
2. Implement `alphapilot/formal_validation/dual_engine_parity.py` using the
   frozen event-engine logic and S01 Freqtrade adapter on a deterministic
   synthetic fixture.
3. Fail closed on any extra/missing event or leg and write
   `dual_engine_readiness_parity.json`.

### 3. Physical non-holdout I/O isolation

1. Add failing tests for exact timerange, data-root allowlist, file allowlist,
   Future Locked OOS denylist, access logging, and path traversal/symlink escape.
2. Implement `alphapilot/formal_validation/freqtrade_io_guard.py` plus
   `scripts/build_non_holdout_freqtrade_data_root.ps1`.
3. Build a tiny fixture-only isolated data root without copying production
   history, run the guard, and write `freqtrade_io_guard_readiness.json`,
   `non_holdout_data_root_manifest.json`, and
   `freqtrade_io_fixture_access_log.json`.

### 4. Future Locked OOS identity and zero-access ledger

1. Add failing tests for a boundary strictly after the frozen data cutoff,
   immutable identity hashes, metadata-only registration, append-only access
   ledger semantics, and zero content reads.
2. Implement `alphapilot/formal_validation/future_locked_oos.py` and a CLI that
   records the route without creating or opening market-data files.
3. Write the identity and zero-access ledger under
   `research/locked_oos/`; keep admission blocked until future data exists and
   the preregistered formal Walk-forward is complete.

### 5. Mechanical readiness rerun

1. Extend readiness checks to consume the four Phase 2 evidence sets.
2. Add failing then passing tests for blocker classification and all hard safety
   counters.
3. Generate a new evidence directory without overwriting Phase 0 or Phase 1.
4. Run formal-validation, exit-policy, full regression, compile, config, safety,
   whitespace, and clean-worktree checks.

## Expected decision

If all four engineering evidence sets pass, `formalExecution` may become ready
for a later, separately authorized research-only sequential Walk-forward.
`lockedOosAdmission` must remain blocked because the Future Locked OOS does not
yet exist and no formal Walk-forward result is produced in this phase.

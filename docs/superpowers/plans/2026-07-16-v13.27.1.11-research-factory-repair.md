# V13.27.1.11 Revised V2 Implementation Plan

> Execute in this order. Every behavior change starts with a failing focused test. The campaign may end with zero formal passes.

## Execution status

Execution stopped correctly at Step 5 on 2026-07-16. The frozen catalog contains
32 hash-verified files, but zero of the two required top-level directions is
formally data-ready. No campaign was preregistered, no holdout was opened, and
Steps 6-9 were not run. See
`reports/research_factory_repair/campaign_stop_decision.json` and
`docs/V13.27.1.11-research-factory-data-readiness-closeout.md`.

## Goal

Repair the AlphaPilot research factory, build a resumable same-exchange derivatives snapshot, preregister two hierarchical top-level families with three directions, run bounded walk-forward and one-shot clean-holdout adjudication, and emit formal evidence only for candidates that satisfy every typed gate.

## Non-negotiable boundaries

- Public historical data only; no credentials, private account data, Demo/Live orders, ARM, Dry Run, Trade API, or Withdraw API.
- Preserve the user's pre-existing dirty Quant report and Console backup directory.
- A/B share `stress_reversal`; C is `cross_sectional_momentum`.
- Event and portfolio gates are separate.
- Next-bar execution, causal availability, purge/embargo, 100% trade identity parity, one-shot holdout, and immutable hashes are mandatory.
- Proxy liquidation and current-Top-N historical universe data cannot produce formal evidence.

## Step 1: Freeze actual closeout and canonical documentation

**Files**

- Create `reports/v13_27_1_11_baseline_closeout_manifest.json`
- Create `reports/v13_27_1_11_baseline_closeout_summary.md`
- Update this design and plan
- Add canonical V2 prompt to the Docs repository

**Actions**

1. Record verified Console, Quant, and Docs heads/tags/push state and preserved dirty paths.
2. Record all acceptance commands and outcomes from the V13.27.1.10 closeout.
3. Hash the closeout files and verify no protected file changed.
4. Run `git diff --check` in Quant and Docs.
5. Commit Quant design/closeout and Docs canonical prompt separately; push both feature branches.

## Step 2: Explicit input-artifact mapping (Commit 1A)

**Tests first**

- Create `tests/research_factory_repair/test_input_artifact_mapping.py`.
- Cover explicit paths, manifest paths, schema-fingerprint selection, exact-name fallback, ambiguity, missing required roles, hashes, and no guessed substitutes.
- Run the focused test and confirm it fails before implementation.

**Implementation**

- Create `alphapilot/research_factory_repair/__init__.py`.
- Create `alphapilot/research_factory_repair/input_artifact_mapping.py`.
- Emit `reports/research_factory_repair/input_artifact_mapping.json` and `.md`.
- Map all required Phase 3/4 roles to immutable actual paths and hashes.
- Stop with `input_mapping_blocked` on missing or ambiguous required artifacts.

**Verify**

```powershell
python -m pytest tests/research_factory_repair/test_input_artifact_mapping.py -q
git diff --check
```

## Step 3: Causal clock, split leakage guard, and typed gates (Commit 1B)

**Tests first**

- Add tests for funding settlement/publication lag, OI lag, PIT snapshot lag, next-bar execution, boundary-crossing event removal, purge, and embargo.
- Add tests proving A/B event gates and C portfolio gates cannot consume each other's metrics.
- Add tests for the explicit public gate field names and fail-closed missing evidence.

**Implementation**

- Add `alphapilot/research_screening/data_availability.py`.
- Add `alphapilot/research_screening/causal_clock.py`.
- Add `alphapilot/research_screening/causal_asof_join.py`.
- Add `alphapilot/research_screening/split_leakage_guard.py`.
- Add `alphapilot/research_screening/gate_schema.py`.
- Add `alphapilot/research_screening/event_strategy_gates.py`.
- Add `alphapilot/research_screening/portfolio_strategy_gates.py`.
- Add `alphapilot/research_screening/formal_data_gate.py`.
- Preserve backward-compatible readers for historical Phase 3 artifacts.

**Reports**

- `reports/research_factory_repair/gate_migration_report.json`
- `reports/research_factory_repair/causality_policy.json`

## Step 4: Parity, hierarchy, capital competition, and holdout controls (Commit 1C)

**Tests first**

- Exact trade identity must be 100%; numeric tolerance applies only to allowed fields.
- A/B hierarchy, event overlap, hierarchical FDR, correlated subfamily selection, shared risk cluster.
- Deterministic signal ranking, concurrent positions, open/same-direction/cluster/symbol risk, beta, and capacity rejection.
- Cluster bootstrap reproducibility and confidence-bound gates.
- Holdout access count zero-to-one, failure closure, and byte-identical technical replay.
- Initial stop freeze, no widening, optional 1R partial, remaining target at least 2R; C has no single-position R target.

**Implementation**

- Add parity/trade-identity modules.
- Add hierarchy, multiple-testing, overlap, capital competition, ranking, clusters, and capacity modules.
- Add cluster bootstrap and uncertainty report modules.
- Add holdout unlock/store and exit-geometry modules.

**Reports**

- Translation parity, benchmark registry, bootstrap policy, holdout policy, and exit-geometry schema under `reports/research_factory_repair/`.

**Commit message**

`Repair research gates causality parity and holdout controls`

## Step 5: Resumable same-exchange data snapshot (Commit 2)

**Tests first**

- API capability audit accurately marks unavailable history.
- Existing rows are scanned before collection.
- Resume starts from the last verified cursor.
- Deduplication key is exchange/instrument/data type/timestamp.
- Only explicit gaps are repaired.
- Request/retry/download/free-disk limits fail safely.
- Missing PyArrow emits deterministic CSV/canonical JSON/SHA-256 and never claims Parquet.

**Implementation**

- Add capability audit, resumable collector, gap repair, and environment manifest modules under `alphapilot/derivatives_data/`.
- Prefer one same-exchange OKX public snapshot. Select Binance before preregistration only if OKX formal history is insufficient; never splice core fields across exchanges.
- Capture perpetual/spot OHLCV, funding, OI, perpetual/spot prices, basis, contract state, PIT liquidity/tradability, and cost evidence where publicly available.
- Declare liquidation and historical PIT limitations honestly.

**Outputs**

- All V2 derivatives-data reports, environment manifest, and immutable snapshot descriptor.

**Stop condition**

- If fewer than two directions are formally data-ready, emit `data_not_ready`, commit evidence, and do not run the campaign.

**Commit message**

`Build resumable same exchange derivatives snapshot`

## Step 6: Preregister hierarchical three-direction campaign (Commit 3)

**Tests first**

- Exactly 2 top-level families, 3 directions, no fourth direction.
- At most 2 variants per direction, 2 exit models per event direction, 20 event backtests, 12 portfolio backtests, and 48 formal trials.
- Research/holdout symbols are disjoint and locked before evaluation.
- All strategy, exit, benchmark, risk, capital, environment, and data hashes are stable.

**Implementation**

- Preregister A1/A2, B1/B2, C1/C2 and the deterministic A/B deduplication selector.
- Freeze a recommended 70/30 research/new-symbol holdout split.
- Freeze capital competition, cost scenarios, ranking, beta, turnover, capacity, bootstrap seeds, purge, embargo, and exits.

**Commit message**

`Preregister hierarchical three direction campaign`

## Step 7: Development prefilter, baselines, and capital competition (Commit 4)

**Tests first**

- Event baselines use matched symbols/months/event counts/horizons/exits/costs.
- C's 20-day momentum baseline uses the same PIT universe, rebalance, exposure, beta, and costs.
- A2 and PIT-proxy C can never become formal passes.

**Run**

- Generate A/B events and C rebalance candidates using research symbols only.
- Select at most one exit model per event direction on development data.
- Record A/B overlap/correlation and hierarchical testing inputs.
- Run deterministic capital competition and capacity filtering.

**Commit message**

`Record event prefilter benchmarks and capital competition`

## Step 8: Real backtests and purged walk-forward (Commit 5)

**Tests first**

- Freqtrade-compatible ledger is required for A/B execution facts.
- Portfolio engine is required for C.
- Fold metrics never include holdout rows.
- Base, 1.5x, and 2x cost scenarios are present.

**Run**

- Execute bounded A/B event backtests and C portfolio backtests.
- Run five-fold purged walk-forward.
- Run at least 5,000 deterministic cluster-bootstrap draws.
- Generate parity, cost, uncertainty, benchmark, overlap, and walk-forward reports.

**Commit message**

`Record Freqtrade and portfolio walk forward results`

## Step 9: One-shot clean holdout and final decisions (Commit 6)

**Precondition**

- Freeze and verify Git, snapshot, preregistration, strategy, exit, benchmark, risk/capital, and environment hashes.
- Confirm holdout access count is zero.

**Run**

- Unlock the new-symbol holdout exactly once.
- Adjudicate A/B with event formal gates and C with portfolio formal gates.
- Apply hierarchical FDR and correlated-subfamily selection.
- Close failed campaigns permanently; permit only audited byte-identical technical replay.
- Emit zero or more `formal_pass_evidence` files. Never import them into Console.

**Commit message**

`Unlock clean holdout once and record final decisions`

## Step 10: Full verification, integration, tags, and final report

```powershell
python -m pytest tests/research_factory_repair -q
python -m pytest tests/derivatives_data -q
python -m pytest tests/research_screening -q
python -m pytest tests/factor_lab -q
python -m pytest tests -q --import-mode=importlib
python -m compileall alphapilot
python -m alphapilot.scripts.validate_config
powershell -ExecutionPolicy Bypass -File scripts\check_safety.ps1
git diff --check
```

1. Verify protected dirty files remain untouched in the user's root worktree.
2. Push each reviewed commit on the feature branch.
3. Merge into clean Quant main without rewriting history; push and create the next unused `v13.27.1.11` tag.
4. Merge/push Docs and create the next unused `v13.27.1.11-docs` tag.
5. Report all 86 V2 self-review fields, raw artifact paths, commit hashes, tags, final Git status, known limitations, and the honest zero/provisional/formal outcome.

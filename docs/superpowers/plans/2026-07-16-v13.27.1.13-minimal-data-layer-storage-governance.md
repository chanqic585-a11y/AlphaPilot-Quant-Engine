# AlphaPilot V13.27.1.13 Minimal Data Layer and Bounded Strategy Funnel

## Objective

Reduce redundant storage inside `D:\Codex-Workspace\回测数据`, freeze a manifest-only minimal research dataset, and run a bounded hypothesis campaign without manufacturing a winner.

## Hard Boundaries

- Cleanup may inspect and mutate only `D:\Codex-Workspace\回测数据`.
- Quant and Docs may receive source code, tests, manifests, and reports.
- Console and App remain read-only.
- No API credentials, private exchange APIs, Demo/Live orders, Withdraw, or runtime ARM.
- Missing evidence remains missing; fixed current cohorts are never presented as point-in-time universes.
- Initial stops never widen and directional targets remain at least 2R.

## Phase 1: Baseline and Storage Governance

1. Repair the pre-existing committed snapshot sidecar hash drift.
2. Add RED tests for root-boundary enforcement, reference protection, duplicate classification, and dry-run/apply parity.
3. Implement `alphapilot.storage_governance` with a reference graph, duplicate classifier, retention policy, cleanup planner, and cleanup executor.
4. Generate storage inventory, duplicate report, retention policy, cleanup plan, and artifact manifest.
5. Apply only entries marked safe after rechecking root, reference count, immutable-evidence status, authoritative-copy hash, and current file hash.

## Phase 2: Minimal Research Data Layer

1. Add RED tests for deterministic fixed-cohort selection, confirmed-candle filtering, common cutoff, and manifest-only snapshots.
2. Select 20-40 mature USDT perpetual contracts from 1h/4h/1d coverage without using strategy returns.
3. Record funding, basis, spread, open-interest, liquidation, PIT-membership, and forward-collection readiness separately.
4. Freeze one shared manifest that references existing files and hashes rather than copying data.
5. Publish coverage, provenance, family eligibility, and forward collection plans.

## Phase 3: Bounded Strategy Funnel

1. Pre-register no more than three hypotheses and one initial implementation per hypothesis.
2. Commit and push the preregistration before reading results.
3. Run an 8-12-symbol deterministic prefilter across multiple regimes.
4. Archive prefilter failures immediately; do not tune them.
5. Run purged five-fold walk-forward and one locked holdout only for survivors, with base, 1.5x, and 2x costs.
6. Permit zero formal passes. Generate no Release unless all frozen gates pass.

## Phase 4: Verification and Delivery

1. Run targeted tests, full repository tests, compileall, configuration validation, safety scan, and diff checks.
2. Verify deleted paths were all inside the authorized data root and were all listed in the applied manifest.
3. Compare disk usage before and after cleanup.
4. Update Quant and Docs navigation and evidence summaries.
5. Commit in reviewable checkpoints, tag, push, and report final clean status and known limitations.

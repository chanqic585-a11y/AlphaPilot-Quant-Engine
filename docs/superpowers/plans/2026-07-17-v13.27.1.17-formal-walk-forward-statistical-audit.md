# V13.27.1.17 S01 Formal Walk-forward and Statistical Audit Plan

> Status: implementation plan. Formal results must not be generated until the implementation commit is published.

## Objective

Run the single preregistered S01 candidate through the frozen five-fold purged Walk-forward, shared-capital replay, benchmark comparison, cost and funding stress, dual-engine parity, and statistical audit without reading Future Locked OOS data or changing any frozen identity.

## Frozen Boundaries

- Candidate count remains 1: `s01_bear_idiosyncratic_selloff_recovery_4h`.
- Parameter, exit-policy, universe, and cost changes remain 0.
- Use the 20-instrument 4h core universe from `minimal_snapshot_785e47b180c17327dcb35e37`.
- Verify every registered source hash before loading data.
- Use exactly five expanding folds with the preregistered purge, embargo, test size, and unused tail.
- No event may cross a fold boundary.
- Future Locked OOS content reads, Release creation, Demo ARM, and orders remain 0.
- A Walk-forward pass cannot enter Locked OOS while `cleanLockedOosAvailable=false`.

## Implementation Tasks

### 1. Formal input contract and snapshot loader

Files:

- Add `alphapilot/formal_validation/formal_input.py`.
- Add `tests/formal_validation/test_formal_input.py`.

Implement canonical preregistration, candidate, snapshot, 20-instrument universe, data-path, SHA256, cutoff, and row-alignment validation. Emit an input mapping and holdout-lineage audit without reading Future Locked OOS content.

### 2. Frozen split manifest and event assignment

Files:

- Add `alphapilot/formal_validation/formal_walk_forward.py`.
- Add `tests/formal_validation/test_formal_walk_forward.py`.

Build the five preregistered folds with the existing purged Walk-forward primitive. Map index boundaries to timestamps, enforce purge and embargo coverage, reject boundary-crossing trades, and emit fold, purge, embargo, and bear-regime causality evidence.

### 3. Shared-capital replay and economic metrics

Files:

- Add `alphapilot/formal_validation/formal_portfolio.py`.
- Add `tests/formal_validation/test_formal_portfolio.py`.

Replay S01 events through the frozen capital-competition policy. Enforce symbol, direction, cluster, portfolio beta, open-risk, and maximum-position limits. Produce fold-level and aggregate PF, average net R, total net R, drawdown, daily returns, concentration, and rejected-event reasons.

### 4. Benchmark, cost, and funding stress

Files:

- Add `alphapilot/formal_validation/formal_stress.py`.
- Add `tests/formal_validation/test_formal_stress.py`.

Use the preregistered same-event fixed-hold benchmark. Run base, 1.5x, and 2x cost scenarios plus conservative funding stress. Keep unavailable historical funding explicitly null and use only the preregistered conservative stress for the funding gate.

### 5. Statistical audit

Files:

- Add `alphapilot/formal_validation/formal_statistics.py`.
- Add `tests/formal_validation/test_formal_statistics.py`.

Generate Newey-West alpha, one-sided p-value, BH family correction, DSR with ten actual trials, PBO, White Reality Check, SPA, bootstrap confidence intervals, and uncertainty diagnostics. Statistical failures must never trigger tuning or a rerun on the same formal window.

### 6. Dual-engine formal parity

Files:

- Extend the formal parity adapter through a new `alphapilot/formal_validation/formal_parity.py`.
- Add `tests/formal_validation/test_formal_parity.py`.

Run the frozen S01 translation against the real 20-instrument formal input and compare canonical signals, entries, exits, legs, R accounting, and costs. Synthetic Phase 2 parity is readiness evidence only and cannot substitute for formal parity.

### 7. Deterministic report bundle and route

Files:

- Add `alphapilot/formal_validation/formal_reporting.py`.
- Add `alphapilot/scripts/run_s01_formal_walk_forward.py`.
- Add `tests/formal_validation/test_formal_reporting.py`.
- Add `tests/formal_validation/test_run_s01_formal_walk_forward.py`.

Write atomically to a staging directory, validate every required artifact, then promote once. The route is derived from frozen gates:

- Economic failure: archive S01 without tuning.
- Economic pass and statistical failure: research-only statistical failure.
- Walk-forward pass with no clean holdout: `walk_forward_research_pass_no_clean_holdout`.
- Locked OOS admission remains blocked in all current routes.

### 8. Publication gate, execution, and verification

Before formal performance execution:

1. Run targeted and full tests.
2. Run compile and safety checks.
3. Commit the implementation only.
4. Obtain explicit approval to push the implementation commit.
5. Verify the remote commit.
6. Run the formal campaign exactly once.
7. Verify artifact hashes, zero Locked OOS access, and zero Release/ARM/order counts.
8. Commit the immutable result bundle; request separate approval before pushing results or tags.

## Verification Commands

```powershell
D:\Codex-Workspace\AlphaPilot-Quant-Engine\.venv\Scripts\python.exe -m pytest tests\formal_validation --import-mode=importlib -p no:cacheprovider
D:\Codex-Workspace\AlphaPilot-Quant-Engine\.venv\Scripts\python.exe -m pytest tests --import-mode=importlib -p no:cacheprovider
D:\Codex-Workspace\AlphaPilot-Quant-Engine\.venv\Scripts\python.exe -m compileall alphapilot
D:\Codex-Workspace\AlphaPilot-Quant-Engine\.venv\Scripts\python.exe -m alphapilot.scripts.validate_config
powershell -ExecutionPolicy Bypass -File scripts\check_safety.ps1
git diff --check
```

## Stop Conditions

- Any snapshot hash mismatch, missing core-universe partition, or split drift.
- Any Future Locked OOS content access.
- Any post-result parameter, exit, universe, or cost change.
- Any missing formal parity, incomplete fold, or unaccounted capital-competition rejection.
- Any attempt to create a Release, ARM Demo, or create an order.

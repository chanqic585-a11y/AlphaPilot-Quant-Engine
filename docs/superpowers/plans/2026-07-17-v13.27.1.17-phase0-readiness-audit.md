# V13.27.1.17 Phase 0 Readiness Audit Implementation Plan

> **Scope:** Readiness evidence only. Do not run formal walk-forward, open Locked OOS, generate a Release, touch Demo execution, or place orders.

**Goal:** Determine whether the frozen V16 S01 candidate can safely enter V13.27.1.17 formal validation, with explicit blockers instead of inferred readiness.

**Baseline:** `v13.27.1.16` at `6d885eee532d45a2c7c1cbeb37c92fe2ac031c3b`.

## Task 1: Freeze and verify V16 identity

- Add tests for the tag/commit identity, campaign/preregistration/snapshot linkage, 21-artifact inventory, and content hashes.
- Implement a read-only metadata loader and artifact verifier.
- Preserve the distinction between the 10 representative symbols and the 20-symbol formal core universe.

## Task 2: Audit Locked OOS lineage without opening data

- Add tests proving the audit accepts metadata only and rejects paths that could expose holdout contents.
- Verify recorded access counts, unlock-ledger state, boundary/hash metadata, and whether clean Locked OOS admission can be claimed.
- Report missing lineage as a blocker; never fill missing evidence by inference.

## Task 3: Audit formal-validation prerequisites

- Check whether candidate events can form the daily statistical panel without computing formal results.
- Check availability of purged walk-forward infrastructure, S01-to-Freqtrade translation, timerange I/O guards, and capital-competition policy.
- Classify each prerequisite as `ready`, `limitation`, or `blocked`.

## Task 4: Emit an auditable evidence bundle

- Write `phase0_readiness_audit.json` and `.md`.
- Write `holdout_lineage_audit.json` and `.md`.
- Write `artifact_manifest.json` with hashes and logical roles.
- Make the final route one of `ready_for_preregistration`, `ready_with_limitations`, or `blocked`.

## Task 5: Verify and checkpoint

- Run focused tests first, then the full test suite with a short external pytest base directory.
- Run `python -m compileall alphapilot`, config validation, safety checks, and `git diff --check`.
- Confirm no Locked OOS access, Release, Demo, order, credential, or Withdraw side effect occurred.
- Commit the Phase 0 implementation and evidence locally; do not push or tag unless separately requested.

# V37E TSMOM Formal 证据闭环

## 结论

本轮工作已完成，不再保留工程或数据阻塞。两条可继续候选均已获得冻结身份下的正式证据，最终路线都是 `archive_s01_current_version`。这是策略表现结论，不是系统故障，也不会通过放宽门槛或结果驱动调参强行晋级。

最终计数：

- 已完成 Formal：2 条。
- 未解决工程阻塞：0。
- 未解决数据阻塞：0。
- 策略表现失败：2 条。
- Release / Demo ARM / 订单：0 / 0 / 0。

## 候选重放

`v37e-bounded-replay-v36-eligible-20260719` 对 9 个候选完成确定性重放：6 个符合重放条件，执行 18 个预注册邻域试验，只有两条 TSMOM 候选形成稳定参数邻域。该阶段没有读取 Formal 结果或锁定 OOS，也没有创建 Release。

## 4h TSMOM

`v35_tsmom_crypto_adaptation` 完成一次 Formal：

- 36 笔交易，8 胜 28 负。
- PF `0.4893`。
- 平均净 R `-0.3602`，总净 R `-12.9677`。
- 最大回撤 `19.2895%`。
- 结论：策略表现失败，当前身份归档。

## 1d TSMOM

原 `v35_tsmom_source_replication` 只有 563 根独立日线，而旧定义需要 1320 根。不存在可以即时补出的额外日历历史，因此没有伪造样本，也没有改写旧身份。

系统改为注册新身份 `v37e_tsmom_daily_capacity_successor`：保持 TSMOM 市场假设，只在读取 Formal 收益前根据时间容量将最大持有期从 180 日改为 18 日。新定义需要 510 根、实际可用 563 根，5 个 Purged Walk-forward 折和资金费率证据均满足运行条件。

该后继候选仅运行一次 Formal：

- 6 笔成交，4 胜 2 负，PF `2.5980`。
- 平均净 R `+0.4389`，总净 R `+2.6334`。
- 净收益 `2.6095%`，最大回撤 `2.4422%`。
- 只有 2 个正向折，要求为 3 个。
- 相对同事件 12-bar 简单基准的总增量净 R 为 `-1.4826`，正增量折为 0。
- 单币种正收益贡献占比 `67.21%`，单月占比 `78.02%`，集中度过高。
- 结论：总收益为正，但跨折稳定性、基准增量和集中度不合格，当前身份归档。

## OOS 说明

两条候选均完成冻结 Formal 的 Purged Walk-forward 样本外折证据。`lockedOosAccessCount = 0` 表示没有在终止路线之后额外读取独立锁定 OOS；这不是遗漏。候选已经在预注册 Formal 门槛下终止，继续读取额外锁定集只会污染后续研究证据，不会改变晋级结论。

## 已解决问题

1. 资金费率就绪检查与 Formal 输入曾使用不同时间窗，现已统一为冻结 Formal 窗口，并加入回归测试。
2. Windows 长路径曾导致 Formal 临时 Artifact 发布失败，现已改为短 staging 根目录后原子发布，并加入回归测试。
3. 两次失败运行的 ledger 和证据均保留；每次修复都创建新的冻结身份，没有重试或改写终态 ledger。

## 下一研究边界

未来如继续研究 TSMOM，必须提出新的可证伪市场假设并创建新预注册身份。不能依据本轮 Formal 结果调整旧身份直至通过，也不能把盈利但不稳健的 1d 结果直接送入 Demo。
